// Ceci est un commentaire sur une ligne
PROG
  var a:int;
  var d:char;

  type h: struct s:int fstruct;

  type t:tableau[2 .. 3] de int;

  var k:bool;
  var c:t;
  var o:float;
  var maVar1Bool:bool;

  procedure g(b:int)
    DEBUT
    FIN;
  fonction g(a:int ; b:int) retourne float
    DEBUT
    FIN;
  fonction h() retourne int
    DEBUT
    FIN;
  fonction h() retourne float
   fonction i(a:float; b:int) retourne char
     DEBUT
     FIN;
  DEBUT
  FIN;

  DEBUT
    tab[f(2)] := 4;
    tab[x.ch1+(y422 + f(y[3,z42].ch2b.ch1,(a==b+2*z)) - 1),42].ch3.ch1[3,a42].ch2.ch4.ch2[f(g(k(3,g(3,14)),x12))+1] := 3,14 ;
  FIN