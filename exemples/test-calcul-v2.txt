// Ceci est un commentaire sur une ligne
PROG
  procedure g(b:5)
    DEBUT
      si ((idriss et antoine) == false)  alors DEBUT FIN sinon DEBUT FIN;
      si ((moi == toi) et (((ok > pasok) ou (!ok < pasok)) == true)) alors DEBUT FIN sinon DEBUT FIN;
    FIN;

  DEBUT
      // TEST expression_booleen   
    si ((idriss <= antoine) et (islem >= antoine) ou (!idriss >= islem)) alors DEBUT FIN sinon DEBUT FIN;
    si ((moi et toi) == nous) alors DEBUT FIN sinon DEBUT FIN;
    si ((moi et toi) >= lui) alors DEBUT FIN sinon DEBUT FIN;
    si ((!oui et !non) >= nope)  alors DEBUT FIN sinon DEBUT FIN;
    si (!ne ou pas)  alors DEBUT FIN sinon DEBUT FIN;
    si ((pas <= !ne) ou (ne >= pas)) alors DEBUT FIN sinon DEBUT FIN;  
    tantque ((idriss <= antoine) et (islem >= !antoine) ou (!idriss >= !islem)) faire DEBUT  FIN;
  FIN
